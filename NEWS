# 0.6.0
* use `cargo metadata` instead of cargo lib. This includs breaking changes.

# 0.5.0
* update cargo version

# 0.4.0
* update cargo version

# 0.3.0
* update cargo version

# 0.2.0
* udate dependencies

# 0.1.0
* first release
* read config from the manifest
* `package.metadata.pack.files`
* `package.metadata.pack.default-packers`
